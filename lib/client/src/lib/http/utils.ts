export const getBaseUrl = () => {
  return "http://localhost:8080"
  // const env = process.env["SVELTE_APP_ENV"]
  // if (env === "production" && process.env["SVELTE_APP_API_URL"] !== undefined) {
  //   return process.env["SVELTE_APP_API_URL"] as string
  // } else {
  //   return "http://localhost:8080"
  // }
}